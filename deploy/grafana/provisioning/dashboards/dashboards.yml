apiVersion: 1

providers:
  - name: Shortener
    orgId: 1
    folder: Shortener
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
